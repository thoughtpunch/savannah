# direnv configuration for Savannah (AI Savannah)
# Activates venv and loads .env automatically on cd.

# Load .env variables into the shell
dotenv_if_exists

# Activate the Python virtual environment
layout python3
# If you prefer a specific venv path:
# source_env_if_exists .venv/bin/activate
